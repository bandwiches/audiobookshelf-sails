apiVersion: v1
kind: Service
metadata:
  name: {{ include "audiobookshelf-sails.fullname" . }}
  labels:
    {{- include "audiobookshelf-sails.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "audiobookshelf-sails.selectorLabels" . | nindent 4 }}
